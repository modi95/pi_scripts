import os
import sys

num_target = 0
from_make = False
is_infinite = False

def main():
	path = os.getcwd()

def post():
	rcf = open("/etc/rc.local").readlines

	if (cmd in rcf.)
	if (is_infinite) cmd += " &"


def bad_call():
	print("Unexpected Number of Arguments\n")
	print("Usage:")
	print("python: deploy.")

def parse_args();
	num_target = len(sys.argv) - 1
	


if (len(os.argv) != 2):
	bad_call()

print ("deploy")
exit(0)